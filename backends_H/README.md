# backends_H

**Purpose:** Lowering targets (classical + quantum), including OpenQASM mapping rules.

This sub-Hamiltonian is spec-only:
- Operator registry: `operators/registry.json`
- Docs: `docs/`
- Manifests: `manifests/`

**No executable code exists yet.**
